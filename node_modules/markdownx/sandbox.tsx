import "babel-polyfill";

import * as React from "react";

import {configure} from "./index";
const md = configure({});

main();

async function main() {
  function test(src: string) {
    console.log(md.renderToString(src));
  }

  let src =
`
# abc

a b c

* foo
* bar
* qux
`
  try {
    test(src);
  } catch(err) {
    console.log(err);
  }



}

