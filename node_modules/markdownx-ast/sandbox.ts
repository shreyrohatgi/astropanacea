
const marked = require("marked");
const options = Object.assign(marked.defaults,{xhtml: true});
const inlineLexer = new marked.InlineLexer([],options);

import { tokenize, parse } from "./parser";

function main() {
  console.log(inlineLexer.rules.tag);
}

main();